import React from "react";
import ReactLoading from "react-loading";

const Loading = ({ spin }) => {
  <ReactLoading type={spin} color="#000" height={667} width={375}></ReactLoading>;
};

export default Loading;
